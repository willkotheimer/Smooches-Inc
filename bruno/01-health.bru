meta {
  name: Health
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/health
}

assert {
  res.status: eq 200
}

tests {
  test("reports ok", function () {
    expect(res.body.status).to.equal("ok");
  });
}
