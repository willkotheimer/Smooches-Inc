# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

.vscode/
package-lock.json
dist/
.firebase/
.env

# local data backups / migration dumps (contain real user emails)
/migration

# dependencies
/node_modules
server/node_modules
/.pnp
.pnp.js

# keep the server's lockfile for reproducible backend installs
!server/package-lock.json

# testing
/coverage

# production
/build

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
